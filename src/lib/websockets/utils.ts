import { WebSocketOptions } from "../types"


export function defineWebSocketProps(props: Partial<WebSocketOptions>): WebSocketOptions {
  let opts: WebSocketOptions = {
  }

  // loop en props. y asigna props[item] a opts[item]


  return opts
}
